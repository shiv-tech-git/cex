cex_is_empty_line()
{
    local line=$1

    if [ -z "${line}" ]
    then
        return 0
    else
        return 1
    fi
}

cex_is_comment()
{
    local line=$1
    
    if [[ "${line:0:1}" == "#" ]]
    then
        return 0
    else
        return 1
    fi
}